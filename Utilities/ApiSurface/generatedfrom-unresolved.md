# generatedFrom - unresolved resources

Generated by `Utilities/Update-ResourceOrigin.ps1`, then edited by hand for the rows that only a
human can decide. A plain re-run keeps every resolved block, because the script skips a resource
whose block already passes `Test-OriginResolved`. The rows below are **not** resolved by that test,
so a re-run recomputes them, and a recorded fact the run cannot derive again is kept rather than
overwritten with a null. That holds under `-Force` too. The reason column then says what was kept.

Each row is a resource whose origin could not be derived from its `commands` array,
`Utilities/cmdlet-mapping.json` and the Graph CSDL. The script never guesses. Resolve a row
by adding the missing fact (a `commands` entry, a mapping entry, a single `@odata.type`
literal) and re-running, or by filling `generatedFrom` by hand, then remove the resource
from the allowlist in `Tests/QA/Microsoft365DSC.SettingsJson.Tests.ps1`.

Unresolved: 22

## Why these stay unresolved

Two groups dominate, and neither is a parser weakness.

**No SDK cmdlet exists.** Ten resources drive a Graph endpoint that the Graph PowerShell SDK never
shipped a cmdlet for, so `cmdletNoun` cannot be filled with a real name. Their `entityType` and
`apiVersion` are recorded from the REST route the module calls, which is enough for the property
differ; only `Test-OriginResolved` still counts them as unresolved because it requires a cmdlet
noun for the Graph workloads. Teaching that function a REST-origin case would resolve all ten
without inventing a single fact.

**The endpoint is not in the CSDL.** A handful of routes the modules call have no entity behind
them in the published metadata, so no entity type can be recorded at all.

| Resource | Workload | Reason |
| --- | --- | --- |
| AADAuthenticationRequirement | MicrosoftGraph | Get-MgBetaUserAuthenticationRequirement: URI '/users/{user-id}/authentication/requirements': 'requirements' is not a navigation property of 'authentication'. |
| AADIdentityProtectionPolicySettings | MicrosoftGraph | The module calls '/beta/identityProtection/policy', which the beta CSDL does not model: 'policy' is not a member of 'identityProtectionRoot'. |
| DefenderRoleDefinition | MicrosoftGraph | entityType 'unifiedRoleDefinition' recorded from '/beta/roleManagement/defender/roleDefinitions'. No SDK cmdlet covers the defender RBAC provider, so cmdletNoun stays null. |
| IntuneDeviceComplianceScriptLinux | Intune | entityType 'deviceManagementReusablePolicySetting' recorded from '/beta/deviceManagement/reusablePolicySettings'. The SDK ships no cmdlet for that collection. |
| IntuneDeviceComplianceScriptWindows10 | Intune | entityType 'deviceComplianceScript' recorded from '/beta/deviceManagement/deviceComplianceScripts'. The SDK ships no cmdlet for that collection. |
| IntuneDeviceControlPolicySetting | Intune | entityType 'deviceManagementReusablePolicySetting' recorded from '/beta/deviceManagement/reusablePolicySettings'. The SDK ships no cmdlet for that collection. |
| IntuneEpmCertificatePolicySetting | Intune | entityType 'deviceManagementReusablePolicySetting' recorded from '/beta/deviceManagement/reusablePolicySettings'. The SDK ships no cmdlet for that collection. |
| IntuneFirewallPolicySetting | Intune | entityType 'deviceManagementReusablePolicySetting' recorded from '/beta/deviceManagement/reusablePolicySettings'. The SDK ships no cmdlet for that collection. |
| IntuneWindowsDataProcessingSettings | Intune | entityType 'dataProcessorServiceForWindowsFeaturesOnboarding' recorded from its singleton route. The SDK ships no cmdlet for it. |
| IntuneWindowsUpdateForBusinessDriverUpdateProfileWindows10 | Intune | entityType 'windowsDriverUpdateProfile' recorded from '/beta/deviceManagement/windowsDriverUpdateProfiles'. The SDK ships no cmdlet for that collection. |
| IntuneWindowsUpdateForBusinessHotpatchProfileWindows10 | Intune | entityType 'windowsQualityUpdatePolicy' recorded from '/beta/deviceManagement/windowsQualityUpdatePolicies'. The SDK ships no cmdlet for that collection. |
| M365DSCGraphAPIRuleEvaluation | MicrosoftGraph | A reporting resource that evaluates a caller supplied Graph query. It has no entity of its own. |
| M365DSCRuleEvaluation | (unknown) | A reporting resource that evaluates other resources' exports. It connects to no workload and has no entity. |
| O365CopilotSettingsPeopleEnhancedPersonalization | MicrosoftGraph | The module calls '/beta/copilot/settings/people/enhancedpersonalization'. 'copilotSetting' carries a 'people' navigation but the CSDL does not model the leaf below it. |
| O365OrgCustomizationSetting | (unknown) | Only a Get cmdlet for OrganizationConfig (name score 0.00) and nothing corroborates it as the resource's entity. The resource connects to several workloads (ExchangeOnline, MicrosoftGraph) and no cmdlet decides between them. |
| O365OrgSettings | (unknown) | The resource spans three workloads (ExchangeOnline, MicrosoftGraph, Tasks) and writes a different entity in each. One origin cannot describe it. |
| O365SearchAndIntelligenceConfigurations | (unknown) | The resource writes MeetingInsightsSettings on ExchangeOnline and both organization insight singletons on MicrosoftGraph. One origin cannot describe it. |
| PPDLPPolicyConnectorConfigurations | PowerPlatforms | Workload recorded. The resource drives the Power Platform REST API directly and no cmdlet noun exists for it. |
| PPPowerAppPolicyUrlPatterns | PowerPlatforms | Workload recorded. The resource drives the Power Platform REST API directly and no cmdlet noun exists for it. |
| PPTenantIsolationSettings | PowerPlatforms | Workload recorded. The resource drives the Power Platform REST API directly and no cmdlet noun exists for it. |
| SPORetentionLabelsSettings | PnP | Workload recorded. The resource calls Invoke-PnPSPRestMethod against a SharePoint REST route and no cmdlet noun exists for it. |
| VivaEngagementRoleMember | MicrosoftGraph | entityType 'engagementRole' recorded from '/beta/employeeExperience/roles'. The SDK ships no cmdlet for that collection. |
